export interface WatchItem {
  id: string
  title: string
  posterUrl: string
  previewPosterUrl?: string
  rating: number
  maxRating?: number
  episodeLabel?: string
  ageRating: string
  episodeCount: string
  genres: string[]
}
